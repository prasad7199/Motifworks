import { ExtractDatePipe } from './extract-date.pipe';

describe('ExtractDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractDatePipe();
    expect(pipe).toBeTruthy();
  });
});
